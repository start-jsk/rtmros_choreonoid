<launch>
  <arg name="KILL_SERVERS" default="false" />
  <arg name="RUN_RVIZ" default="true" />
  <include file="$(find hrpsys_ros_bridge_jvrc)/launch/jaxon_jvrc_startup.launch" >
    <arg name="KILL_SERVERS" default="$(arg KILL_SERVERS)" />
    <arg name="NOSIM" default="true" />
  </include>
  <include file="$(find hrpsys_ros_bridge_jvrc)/launch/jaxon_jvrc_ros_bridge.launch">
    <arg name="RUN_RVIZ" default="$(arg RUN_RVIZ)" />
  </include>

  <sphinxdoc><![CDATA[

.. code-block:: bash

  rosrun roseus roseus `rospack find hrpsys_ros_bridge_jvrc`/euslisp/jaxon_jvrc-pickup.l

This launch file shows an example of ros bridge for open jaxon_jvrc robot, `jaxon_jvrc.launch` file launches folloiwng two program

1. `roslaunch hrpsys_ros_bridge_jvrc jaxon_jvrc_startup.launch NOSIM:=true` : launch simulated robot environment, if you use real robot, you don't need this line

2. `roslaunch hrpsys_ros_bridge_jvrc jaxon_jvrc_ros_bridge.launch` : launch rtm ros bridge

After that, we need to invoke the client program. Following program is the example of euslisp language

3. `rosrun roseus roseus jaxon_jvrc-interface.l` : sample euslisp script to control the robot.

  ]]></sphinxdoc>
</launch>


